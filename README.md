# Java Project Matching App

Javaの自己学習で習得した内容を使って開発した、案件マッチングアプリです。

案件情報とエンジニアのスキル・希望条件を比較し、
案件ごとのマッチスコアを確認できます。

現在は、Java基礎の理解を目的とした **Ver0.1 コンソール版** が完成しています。

---

## 制作目的

Javaの自己学習で学んだ内容を、実際のアプリケーション開発で使用し、
基礎構文への理解を深めることを目的として制作しました。

最初から完成形のWebアプリを作るのではなく、

```text
Ver0.1
Java基礎によるコンソール版
↓
Ver0.2
オブジェクト指向版
↓
Ver1.0
Webアプリ版
```

のように、Javaの学習段階に合わせて同じアプリを段階的に発展させる予定です。

---

## Ver0.1で実装した機能

- メインメニュー
- 案件一覧表示
- 必須スキルによる案件検索
- リモート可能案件の絞り込み
- エンジニア情報と案件条件を比較したマッチスコア計算
- マッチ度判定
  - 80〜100点：高マッチ
  - 60〜79点：候補
  - 0〜59点：低マッチ
- メニューへの復帰
- アプリ終了

---

## 使用したJavaの内容

Ver0.1では、主に以下のJava基礎を使用しています。

- 変数
- `String`
- `int`
- `boolean`
- 配列
- 演算子
- `if / else if / else`
- `switch`
- `while`
- `for`
- `Scanner`

現在の学習範囲で自分が処理を説明できることを優先し、
独自クラスやデータベースなどは使用していません。

---

## データ管理

Ver0.1では、案件情報を平行配列で管理しています。

```java
String[] projectNames;
String[] requiredSkills;
String[] optionalSkills;
int[] requiredYears;
String[] locations;
boolean[] remoteAvailable;
```

同じ添字が同じ案件の情報を表します。

エンジニア側についても、

```java
String[] engineerSkills;
int[] experienceTypes;
int[] skillYears;
```

を使用して、スキル名・経験区分・実務経験年数を対応させています。

---

## マッチスコア

案件ごとに100点満点で計算します。

| 評価項目 | 最大点 |
|---|---:|
| 必須スキル | 50点 |
| 尚可スキル | 15点 |
| 実務経験年数 | 15点 |
| 勤務地 | 10点 |
| リモート条件 | 10点 |
| 合計 | 100点 |

### 必須スキル

- 実務経験あり：50点
- 学習経験のみ：25点
- スキルなし：0点

### 尚可スキル

- 実務経験あり：15点
- 学習経験のみ：8点
- スキルなし：0点

### 実務経験年数

必要経験年数を満たしている場合は15点です。

必要年数を満たしていない場合は、

```text
実務経験年数 × 15 ÷ 必要経験年数
```

で部分点を計算します。

学習経験のみの場合は、実務経験年数としては扱いません。

---

## 実行方法

### 動作環境

- Java：OpenJDK 26.0.2.1
- JDK：Eclipse Temurin
- エディタ：Visual Studio Code

### コンパイル

```bash
javac src/Main.java
```

### 実行

```bash
java -cp src Main
```

---

## 操作イメージ

```text
================================
  案件マッチングアプリ Ver0.1
================================
1. 案件一覧を表示
2. 必須スキルで検索
3. リモート可能案件を表示
4. マッチスコアを表示
0. 終了

番号を入力してください:
```

各処理の結果を確認した後、Enterキーを押すとメインメニューへ戻ります。

---

## 学習したこと・振り返り

### Java基礎を実際の処理に使う

Javaの基礎を学習した後、その内容を使って実際に動くものを作ってみたいと考え、本アプリの開発を始めました。

まだJava学習の初期段階のため、Ver0.1はコンソールアプリとして制作しています。

実装方法を調べたりAIのサポートを利用したりしながら進めていますが、
完成コードをそのまま使用するのではなく、まず自分で処理を考えてから実装することを意識しています。

実際にコードを書くことで、教材を見ているだけでは気付かなかった部分を理解する機会が増えました。

### boolean型への理解

リモート可能案件を絞り込む際、当初は以下のように書いていました。

```java
if (remoteAvailable[i] == true) {
```

しかし、`remoteAvailable[i]` 自体が `boolean` 型であるため、

```java
if (remoteAvailable[i]) {
```

だけで同じ判定ができることを理解しました。

構文として使用できることと、その型や仕組みを理解して使用できることは別だと感じた部分で、
boolean型について改めて理解を深めるきっかけになりました。

### マッチスコア計算

Ver0.1の中で最も難しかったのが、マッチスコアの計算処理です。

まず、

```text
案件をfor文で1件ずつ処理
↓
案件ごとにscoreを0へ初期化
↓
各条件をif文で判定
↓
条件に応じてscoreへ加点
```

という流れに分解すると、処理を考えやすくなりました。

一方で、必須・尚可スキルについては、

- スキルを保有しているか
- 実務経験か学習経験か

を判定する必要があります。

さらに経験年数では、

- 必須スキルがエンジニアのどのスキルに対応するか
- 実務経験があるか
- 必要経験年数を満たしているか
- 不足している場合に何点与えるか

まで考える必要があり、複数の条件を組み合わせる難しさを感じました。

特に最初は、

```text
案件の添字 i
```

と、

```text
エンジニアのスキルの添字
```

を同じものとして比較してしまっていました。

その後、

```text
i = 案件を処理するための添字
j = エンジニアのスキルを探すための添字
```

として役割を分け、
案件の必須スキルをエンジニアのスキル一覧から検索してから、
そのスキルの経験区分や実務経験年数を確認するよう修正しました。

この実装を通して、for文やif文を単体で理解するだけでなく、
複数の配列や条件を組み合わせて処理する考え方を学ぶことができました。

---

## 動作確認

Ver0.1完成時に以下を確認しています。

- 全案件を一覧表示できる
- 存在する必須スキルで検索できる
- 存在しない必須スキルの場合に0件表示できる
- リモート可能案件だけを表示できる
- 全案件のマッチスコアと判定を表示できる
- 範囲外のメニュー番号に案内を表示できる
- 各処理後にメニューへ戻れる
- `0`を入力すると正常終了する

---

## 今後の予定

### Ver0.2

Javaの学習が進んだ段階で、Ver0.1をオブジェクト指向の構成へリファクタリングする予定です。

主な予定：

- メソッドへの処理分割
- `Project` クラスの作成
- `Engineer` クラスの作成
- 平行配列からオブジェクトへの変更
- コレクションの利用
- 例外処理
- 入力チェック
- 必須・尚可スキルの複数登録

### Ver1.0

最終的には以下の構成によるWebアプリへの発展を予定しています。

- Java
- Spring Boot
- PostgreSQL
- React
- TypeScript

案件の登録・検索・マッチング・ステータス管理などを行えるWebアプリを目標としています。

---

## 開発方針

本成果物では、完成速度よりも、

**現在学習した内容を自分で理解し、自分の言葉でコードを説明できること**

を重視しています。

新しいJavaの内容を学習するたびに既存コードへ反映し、
どのようにコードが変化していくのかもGitHub上に残していく予定です。

---

## 注意事項

本アプリに使用している案件・企業・人物などの情報は、
すべて学習用に作成した架空のサンプルデータです。

実在する企業・人物・案件とは関係ありません。

